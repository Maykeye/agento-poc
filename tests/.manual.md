@project_dir .

API test and fixing through proxy

As of now server sometimes passes tool calls into the content and reasoning content. 
This is not reliable to test: it works in 99% of cases. So we need to test it somehow So we need to test it somehow
We need to pass it by building a proxy that moves it into function tool.


Ideally I want you to show show me example how you'll be calling tool call of function `demo_func` of func `parm_name` and value `value`.
You do not have this function. And it's not needed, as we testing proxy.

Therefore I ask "fake" function call by using usual function call, but replacing syntax of proxy test: in `<`, `tool_call`, `>`, `<`, `function`, `>`, `<`, `parameter`, `>`
* always replace `<` with `(` 
* always replace and `>` with `)`
* Make sure you include new lines:
example
    (tool_call)
    (function=read_file)
    (parameter=path)
    foo.txt
    (/parameter)
    (/function)
    (/tool_call)
* Make sure NOT to include white space as I give in example. Proxy will hunt for tokens at new line.

My proxy will allow to for test to detect it and fix the call from content.
