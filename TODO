Patch tool -- do manually

 I see the issue - writing empty files causes an error because the code tries to access `lines[0]` for the first written line. Let me fix the test to write files with at least one line of content:

