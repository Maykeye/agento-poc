<INTRO>
<FILES_INFO>
Note: Do not read all the files. They are given to you to avoid `ls`ing around and reading unneeded files

# Context mode

- `context/context.py` has methods for getting, setting context handler
- `context/raw.py` most basic, raw implementation
- `context/prefix.py` deprecated prefix implementation which keeps history of changes in the first message
- `context/suffix.py` current advanced suffix implementation which keeps history of changes in last messages and prunes earlier messages

# Tools

- Tool base class `Tool` is defined in tool.py
- `tool_io` contains tools for common operations
- `tool_editor` contains tools for editor mode
- `tool_sh` contains tools for running other programs(cargo, git, etc)
- `tool_fork.py` contains forking llm for spawning subagents
- `tool_edit_patch.py` contains tool that applies patch generated by LLM

# SQL

Sql utilities are stored in `utilsql.py`

# Tests

- Please create all test classes based on TestBase from tests/test_helper.py
- instead of self.assertIsInstance(var, type) use assert isinstance. 
Example: `assert isinstance(result, dict)`

## Tests for tools

### Files set up and cleared by TestBase

- Use FILE_FOO as primary test file unless specified the other.
- If you need the second file(e.g. empty file), use FILE_BAR and write content there

### Context tests

#### Basic
- tests/test_helper.py

#### Context
- tests/test_context.py
- tests/test_context_prefix.py
- tests/test_context_raw.py
- tests/test_context_suffix.py
- tests/test_context_suffix_prune.py
#### Diff tests
- tests/test_edit_diffpatch.py

#### Editor mode test
- tests/test_editor.py

#### Tests for more advanced tools from tool_io
- tests/test_fold.py
- tests/test_fold_regex.py
- tests/test_ls.py
</FILES_INFO>

- For editing files please use edit_file
- When you create a file, in the end add it to git
</INTRO>
